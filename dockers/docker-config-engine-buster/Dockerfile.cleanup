# Base docker build
FROM docker-config-engine-buster-sonic:latest

# Copy the cache data to host
From scratch as output
COPY --from=docker-config-engine-buster-sonic:latest /cache.tgz cache.tgz

# Clean up the cache data
FROM docker-config-engine-buster-sonic:latest as final
RUN rm /cache.tgz

