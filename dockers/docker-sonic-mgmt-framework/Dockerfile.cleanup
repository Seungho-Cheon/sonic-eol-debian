# Base docker build
FROM docker-sonic-mgmt-framework-sonic:latest

# Copy the cache data to host
From scratch as output
COPY --from=docker-sonic-mgmt-framework-sonic:latest /cache.tgz cache.tgz

# Clean up the cache data
FROM docker-sonic-mgmt-framework-sonic:latest as final
RUN rm /cache.tgz

