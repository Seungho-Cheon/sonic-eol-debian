# Base docker build
FROM docker-base-buster-sonic:latest

# Copy the cache data to host
From scratch as output
COPY --from=docker-base-buster-sonic:latest /cache.tgz cache.tgz

# Clean up the cache data
FROM docker-base-buster-sonic:latest as final
RUN rm /cache.tgz

